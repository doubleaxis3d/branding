# DOUBLE AXIS — QA 리포트

- **대상 파일**: `index.html` (단일 파일, 10.08 MB)
- **검사일**: 2026-07-31
- **검사 방식**: Chromium(Playwright) 자동 계측. 12개 뷰포트 × 전체 스크롤, DOM 실측값 기준
- **검사 환경 한계**: 헤드리스 Chromium에는 H.264 디코더가 없어 히어로 영상 **재생 자체는 미검증**(`readyState 0` / `error 4`). 영상 관련 항목은 코드·메타데이터 기준 판단임

---

## 1. 요약

| 구분 | 결과 |
|---|---|
| JS 에러 | **0건** (12개 뷰포트 전부) |
| 콘솔 에러 | **0건** |
| 가로 스크롤 넘침 | **0건** (320px~2560px) |
| CLS (레이아웃 이동) | **0** |
| 롱태스크(>50ms) | **0건** |
| 중복 ID / alt 없는 img / 빈 링크 | 각 **0건** |

구현 품질은 전반적으로 양호. 발견된 이슈는 대부분 **미완성 콘텐츠**와 **배포 준비 항목**이며, 코드 결함은 소수임.

---

## 2. 심각도별 이슈

### 🔴 Critical — 사이트 기능이 성립하지 않음

#### ~~C-1. 문의 폼이 전송되지 않음~~ → **해결됨 (2026-08-04)**
- Netlify Forms 연동. 수신 주소 `doubleaxis.3d@gmail.com`
- 필수값 검증, 인라인 에러, 전송 상태 UI 추가
- 허니팟(`company-website`) 스팸 대책 추가
- 전송 실패 시 작성 내용이 담긴 `mailto:` 폴백 제공
- **남은 작업**: 최초 배포 후 Netlify 대시보드에서 알림 이메일 설정 필요

#### C-2. 대체 연락 수단 부재
- `mailto:` / `tel:` / 소셜 링크 **전무**
- 외부 링크 **0개**
- **영향**: C-1과 결합해 **연락 경로가 완전히 차단됨**

#### C-3. Work 메뉴에 작업물 없음
- `#work` 앵커가 가리키는 곳은 스테이트먼트(선언문) 섹션
- 케이스 스터디·포트폴리오 항목 없음
- **영향**: 포트폴리오 사이트에 포트폴리오가 없음

#### C-4. 개인정보처리방침 없음
- 동의 문구에 "in accordance with our Privacy Policy" 명시되어 있으나 **해당 페이지·링크 없음**
- 이름·이메일 수집 예정이므로 법적 리스크
- **조치**: 방침 페이지 작성 + 링크 연결, 또는 문구 수정

---

### 🟠 High — 실사용에 직접적 영향

#### H-1. 파일 용량 10.08 MB (영상 9.27 MB가 base64 인라인)

| 구성 | 크기 |
|---|---|
| 마크업 + CSS + JS | 67 KB |
| 폰트·이미지 (base64 6개) | 0.74 MB |
| **히어로 영상 (base64)** | **9.27 MB** |

base64는 원본 대비 **33% 오버헤드**가 발생하며, HTML 파싱 완료 전까지 아무것도 렌더되지 않음.

**첫 화면까지 대기 시간 (실측 용량 기준 산출)**

| 회선 | 현재 | 영상 외부 분리 시 |
|---|---|---|
| 광랜 100Mbps | 0.8초 | 0.1초 |
| 4G LTE 25Mbps | 3.2초 | **0.3초** |
| 4G 혼잡 8Mbps | 10.1초 | **0.8초** |
| 3G 1.6Mbps | 50.4초 | **4.0초** |

**권장**: mp4를 외부 파일로 분리 → HTML 0.8MB 이하. 모바일용 저해상도 버전 별도 제공.

#### H-2. 서비스 카드가 클릭을 유도하나 동작 없음
- `cursor:pointer`, 커스텀 커서 라벨, "View →" 표기 존재
- 실제 링크 아님 → 클릭해도 반응 없음
- **영향**: 고장으로 인지될 소지. C-3 해결과 함께 처리 권장

#### H-3. 히어로 영상이 세로 화면에서 대부분 잘림
- 영상 16:9, 세로 화면 약 9:19.5 → `object-fit:cover` 특성상 **가로의 약 25%만 노출**
- 화면 정보량 기준 약 **20%만 표시**
- 피사체가 x=0.29(살쾡이), x=0.72(토끼)에 분산 → `object-position` 조정으로는 한쪽만 선택 가능
- **시도 후 원복**: 레터박스 배치 및 크롭 위치 조정 모두 시각적으로 부적합 판정
- **권장**: 원본 4K에서 세로 구도로 재렌더링(CSS만으로는 해결 불가)

#### H-4. `<video poster>` 미지정
- 로딩 중 또는 디코드 실패 시 **검은 화면** 노출
- **조치**: 대표 프레임을 poster로 지정

---

### 🟡 Medium — SEO·공유·구조

#### M-1. `<head>` 메타태그 대부분 누락

| 항목 | 상태 |
|---|---|
| charset / viewport / title | ✅ |
| **description** | ❌ |
| **og: (OG 태그)** | ❌ |
| **twitter: (트위터 카드)** | ❌ |
| **canonical** | ❌ |
| **favicon / apple-touch-icon** | ❌ |
| **theme-color** | ❌ |
| robots / sitemap.xml | ❌ |

**영향**: 카카오톡·슬랙·인스타 등에 링크 공유 시 **미리보기 카드가 표시되지 않음**.

#### M-2. `<h1>` 2개
- 히어로 "DOUBLE AXIS", CTA "Let's Start Something"
- HTML5 규격상 위반은 아니나 SEO 관점에서 CTA는 `<h2>` 권장

#### M-3. `<main>` 랜드마크 없음
- `header` / `nav` / `footer` / `section` / `article`은 적절히 사용 중
- `<main>` 부재로 스크린리더의 본문 건너뛰기 미지원

#### M-4. 404 페이지, 분석 도구 없음

---

### 🔵 Low — 접근성·마감

#### L-1. 데스크톱 터치 타겟 미달

| 요소 | 크기 | 기준(44×44) |
|---|---|---|
| 네비게이션 링크 | 40×16 / 66×16 / 62×16 | ❌ |
| 폼 입력칸 | 315×**18** | ❌ |
| 동의 체크박스 | **13×13** | ❌ |
| Send 버튼 | 92×**39** | ❌ |

모바일(≤900px)은 이미 보정 완료(링크 43px, 입력칸 30px, 버튼 46px, 체크박스 20px). **데스크톱은 미적용** — 터치스크린 노트북 사용자에 영향.

#### L-2. 11px 미만 텍스트 다수

| 요소 | 크기 |
|---|---|
| `statement-system` (모바일) | **7px** |
| `axis-readout`, `peek-wall-note` | 8px |
| `fine-print` (개인정보 동의문) | **8.5px** |
| `peek-label`, 기타 | 9px |
| `fnote` (저작권) | 10px |

의도된 마이크로 타이포그래피로 보이나, **`fine-print`는 법적 고지 성격**이므로 8.5px는 재고 필요.

#### L-3. `outline:none` 요소 5개
- `:focus-visible` 대체 스타일이 있는지 요소별 확인 필요

#### L-4. 인쇄 스타일 없음
- 인쇄 시 배경 `#0a0a0c` 그대로 → 검은 페이지 출력

#### L-5. 브랜드 정체성 불일치

| 위치 | 표현 |
|---|---|
| `<title>` | Visual Artist / Technical Designer (**개인**) |
| 본문 | "**We** create refined..." (**팀**) |
| 푸터 | Double Axis **Studio** |

1인/스튜디오 중 하나로 통일 권장.

#### L-6. `lang="en"` — 국내 대상이면 한국어 버전 검토

---

## 3. 정상 동작 확인 항목

### 반응형 — 12개 뷰포트 전부 이상 없음
320 / 360 / 393 / 412 / 560 / 768 / 780(가로) / 1024 / 1280 / 1440 / 1920 / 2560

### 인터랙션

| 항목 | 결과 |
|---|---|
| 모바일 서비스 아코디언 | 카드 전환 시 높이 변동 **0px** (475px 고정) |
| 데스크톱 서비스 아코디언 | `360/360/360/360` → 포커스 시 `302/533/302/302` |
| 네비게이션 앵커 (모바일) | 3개 전부 헤더(67px) 아래 정상 정지 (79~100px) |
| Top 버튼 | 히어로 통과 후 표시, 클릭 시 `scrollY=0` 복귀, 푸터 겹침 회피 |

### 접근성

| 항목 | 결과 |
|---|---|
| 탭 순서 | 논리적 — nav 3 → 서비스 카드 4 → 폼 5 → Send → Top |
| JS 비활성화 | 콘텐츠 **1660자 정상 노출**, 서비스 섹션 정상 표시 |
| `prefers-reduced-motion` | 에러 없음, 모든 요소 `opacity:1` |
| 라벨-입력 연결 | 전부 `for`/`id` 연결됨 |
| 중복 ID / alt 누락 / 빈 링크 | 각 0건 |

### 성능

| 지표 | 값 |
|---|---|
| First Contentful Paint | 280ms (로컬) |
| DOM Interactive | 503ms |
| Load | 652ms |
| **CLS** | **0** |
| **롱태스크(>50ms)** | **0건** |

스크롤 연동 영상 재생 중에도 메인 스레드 블로킹 없음.

---

## 4. 권장 처리 순서

1. ~~**C-1**~~ 완료 / **C-2** — 이메일·소셜 등 대체 연락 수단 노출
2. **C-3, H-2** — Work 섹션 신설 및 서비스 카드 연결
3. **H-1, H-4** — 영상 외부 분리 + poster 지정
4. **C-4, M-1** — 개인정보처리방침 + 메타태그 세트
5. **L-1, L-2** — 데스크톱 터치 타겟, `fine-print` 크기
6. **M-2, M-3, L-4, L-5** — 마감 항목

---

## 5. 검토 요청 사항 (GPT 참고용)

아래는 자동 계측만으로 판단이 어려운 항목입니다. 함께 검토 바랍니다.

1. **H-3 히어로 영상 크롭** — CSS 접근(레터박스, `object-position` 조정)은 두 차례 시도 후 시각적 부적합으로 원복. 영상 재렌더링 외 대안이 있는지?
2. **L-2 마이크로 타이포그래피** — 7~9px는 디자인 의도로 보이나 판독성 한계. 어느 선까지 허용할지?
3. **CTA 헤드라인 겹침** — "SOMETHING"의 마지막 G가 연락처 카드에 절반 가려짐(의도된 편집 디자인). 의도가 충분히 읽히는지?
4. **히어로 길이** — `220svh`로 전체 문서의 약 33% 차지. 5초 영상 대비 적정한지?
5. **L-5 브랜드 정체성** — 개인/스튜디오 중 어느 쪽으로 통일할지?
